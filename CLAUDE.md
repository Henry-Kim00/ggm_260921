@AGENTS.md

# 프로젝트 소개

이 폴더는 **개발 연습용** 프로젝트입니다. 사용하는 기술은 다음과 같습니다.

- **Next.js**: 웹사이트(화면)를 만들 때 쓰는 프로그램 도구(프레임워크)입니다.
- **Supabase**: 회원가입/로그인, 데이터 저장을 대신 처리해주는 온라인 서비스(데이터베이스)입니다.
- **Vercel**: 완성된 웹사이트를 인터넷에 올려서(=배포) 누구나 주소로 접속할 수 있게 해주는 서비스입니다.

# Claude Code 작업 규칙 (이 프로젝트 전용)

## 1. 개발 용어는 쉽게 설명하기
- 전문 용어가 나오면 비개발자도 이해할 수 있게 풀어서 설명한다.
  - 예: "배포(deploy)" → "완성된 결과물을 인터넷에 올려서 누구나 볼 수 있게 만드는 것"
  - 예: "빌드(build)" → "작성한 코드를 실제로 작동하는 프로그램으로 만드는 과정"
- 용어를 꼭 써야 할 때는 옆에 괄호로 쉬운 뜻을 같이 적는다.

## 2. 진행 상황을 자주 알려주기
- 작업을 시작할 때, 진행 중일 때, 끝났을 때 지금 무엇을 하는지/했는지 짧게 알려준다.
- 조용히 여러 단계를 한 번에 처리하지 않고, 중간중간 눈에 보이게 공유한다.

## 3. 사용자가 지시한 것만, 단계적으로 진행하기
- 요청받지 않은 작업은 먼저 하지 않는다.
- 작업은 작은 단계로 나누어 하나씩 진행한다.
- 지시 범위를 벗어나는 추가 기능/개선/정리가 필요해 보이면, 실행하지 말고 "이런 것도 필요해 보이는데 진행할까요?"처럼 먼저 제안만 한다.

## 4. 할 수 있는 일은 최대한 직접 처리하기
- 코드 작성, 파일 수정, 설정 변경 등 Claude Code가 할 수 있는 작업은 최대한 대신 처리한다.
- 사람이 반드시 직접 해야 하는 일(예: 결제 정보 입력, 외부 서비스 계정 로그인, 이메일 인증 클릭, 비밀번호 입력 등)만 골라서 안내한다.
- 안내할 때는 "왜 필요한지 → 어떻게 하면 되는지" 순서로 쉽게 설명한다.

## 5. 작업이 끝나면 바로 확인할 수 있게 열어주기
- 개발/수정 작업을 마치면, 결과를 바로 눈으로 확인할 수 있도록 미리보기 화면(브라우저 등)을 열어서 보여준다.
- 확인 없이 "완료했습니다"라고 말로만 끝내지 않는다.
